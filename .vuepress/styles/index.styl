/* animate */
@media (print), (prefers-reduced-motion: reduce) {
  .hero img  {
    animation-duration: 1ms !important;
    transition-duration: 1ms !important;
    animation-iteration-count: 1 !important; 
  }
}

@keyframes fadeInDownBig {
  from {
    opacity: 0;
    transform: translate3d(0, -2000px, 0);
  }
  to {
    opacity: 1;
    transform: translate3d(0, 0, 0);
  }
}

.hero img {
  animation-duration: 2s;
  animation-fill-mode: both;
  // animation-delay: 1s;
  // animation-iteration-count: infinite;
  animation-name: fadeInDownBig;
  // animation-duration: 1.3s;
  animation-timing-function: ease-in-out;
}

body{
    font-family: 'Roboto';
}

h1, h2{
    font-family: 'Oswald';
}

h1 {
    font-variant: small-caps;
}

h2 {
    text-transform: uppercase;
}

h3 {
    opacity: 0.9;
}


h1, h2, h3 {
    border-bottom: none !important;
}

.content h1:after,.content h2:after,.content h3:after {
    content: ""; /* This is necessary for the pseudo element to work. */ 
    display: block; /* This will put the pseudo element on its own line. */
    margin: 0 0; /* This will center the border. */
    width: 5%; /* Change this to whatever width you want. */
    padding-top: 20px; /* This creates some space between the element and the border. */
    border-bottom: 3px solid #272D47; /* This creates the border. Replace black with whatever color you want. */
}

/* Hero */
.hero {
    /*height: 100vh;*/
    background-image: linear-gradient(to bottom, rgba(255, 255, 255, 0.9), rgba(255, 255, 255, 1)), url("/front.jpg");
    background-size: cover;
    padding-top: 9%;
    padding-bottom: 3rem;
}

.hero {
    //min-height: 100vh;
}

#main-title, .description, .contact{
    background-color: #272D47;
    margin: 0;
    padding: 1.5rem;
    color: white !important;
}


.contact a {
    color: white;
}
.contact strong {
    font-size: 1.8em;
    font-weight: lighter;
}

#main-title{
    font-size: 1.6em;
    text-transform: uppercase;
    font-weight: lighter;
    margin-top: 5rem !important;
}

/* Tagline */
.description {
  min-height: 2em;
  margin: 0 !important;
  margin-bottom: 5rem !important;
  max-width: none !important;
  width: auto !important;
  font-weight: bold;
  font-size: 2.4em !important;
}

.description::before {
  content: "\201C";
  font-family: georgia, serif;
  font-size: 2em;
  height: .4em;
  // line-height: 0.6;
}

.features {
    margin-bottom: 5rem !important;
}

.prestations h2{
    margin-top: 3rem !important;
}

.mentions-legales .content__default{
    margin-top: 10%;
    background-color: rgba(169,186,199,0.1);
}

.mentions-legales h2{
    margin-top: 1rem !important;
}

.mentions-legales p {
    margin-left: 2rem;
}

.mentions-legales h3 {
    margin-left: 1rem;
}

.sidebar{
    // background-color: rgba(169,186,199,0.2);
}
